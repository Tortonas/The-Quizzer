{% extends 'base.html.twig' %}

{% block title %}{{ user.username }} profilis - Quizzer{% endblock %}
{% block description %}{{ user.username }} nario profilis su visa detalia informacija apie atsakytus klausimus bei kontaktinę informaciją.{% endblock %}

{% block body %}
    <div class="card card-size transparent mx-auto mt-5">
        <div class="card-body">
            <h5 class="card-title text-center">Labas, {{ user.username }} !</h5>
            <p class="card-text text-center">Pas mus nuo - {{ user.registerAt|date('Y-m-d') }} 😊</p>
        </div>
        <ul class="list-group list-group-flush">
            <li class="list-group-item d-flex justify-content-between align-items-center bg-transparent">
                Elektroninis paštas
                <span class="badge badge-warning badge-pill">{{ user.email }}</span>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center bg-transparent">
                Atsakyti klausimai per paskutiniąją savaitę
                <span class="badge badge-warning badge-pill">{{ weeklyAnswers }}</span>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center bg-transparent">
                Atsakyti klausimai per paskutinįjį mėnesį
                <span class="badge badge-success badge-pill">{{ monthlyAnswers }}</span>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center bg-transparent">
                Atsakyti visų laikų klausimai
                <span class="badge badge-danger badge-pill">{{ allTimeAnswers }}</span>
            </li>
        </ul>
        <div class="card-body">
            <a href="{{ path('app_profile_edit') }}?v=p" class="card-link">Pasikeisti slaptažodį</a>
            <a href="{{ path('app_profile_edit') }}?v=e" class="card-link">Pasikeisti el. paštą</a>
        </div>
    </div>
{% endblock %}
