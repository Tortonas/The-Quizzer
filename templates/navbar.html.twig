<nav class="navbar navbar-expand-lg navbar-light" style="background-color: rgba(128,191,243, 0.2);">
    <a class="navbar-brand" href="{{ path('app_home') }}">The Quizzer!</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>


    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_home') }}">Namai</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_results') }}">Rezultatai</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_faq') }}">D.U.K.</a>
            </li>
            {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_profile') }}">Profilis</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('admin') }}">Administracija</a>
                    </li>
                {% endif %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}">Atsijungti</a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">Prisijungti</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_register') }}">Registruotis</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>